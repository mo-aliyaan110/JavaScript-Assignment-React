function changeBG(color){
    document.getElementById('parent').style.backgroundColor = color;

    // document.children.style.background = color;
    // // document.firstElementChild.style.background = color;
    // document.childNodes(1).style.background = color;
}
    
